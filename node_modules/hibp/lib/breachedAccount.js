"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _fetchFromApi = _interopRequireDefault(require("./internal/haveibeenpwned/fetchFromApi"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * Fetches breach data for a specific account.
 *
 * @param {string} account a username or email address
 * @param {Object} [options] a configuration object
 * @param {string} [options.domain] a domain by which to filter the results
 * (default: all domains)
 * @param {boolean} [options.truncate] truncate the results to only include
 * the name of each breach (default: false)
 * @returns {Promise} a Promise which resolves to an array of breach objects
 * (or null if no breaches were found), or rejects with an Error
 * @example
 * breachedAccount('foo')
 *   .then(data => {
 *     if (data) {
 *       // ...
 *     } else {
 *       // ...
 *     }
 *   })
 *   .catch(err => {
 *     // ...
 *   });
 * @example
 * breachedAccount('bar', { truncate: true })
 *   .then(data => {
 *     if (data) {
 *       // ...
 *     } else {
 *       // ...
 *     }
 *   })
 *   .catch(err => {
 *     // ...
 *   });
 * @example
 * breachedAccount('baz', { domain: 'adobe.com', truncate: true })
 *   .then(data => {
 *     if (data) {
 *       // ...
 *     } else {
 *       // ...
 *     }
 *   })
 *   .catch(err => {
 *     // ...
 *   });
 * @alias module:breachedAccount
 */
var breachedAccount = function breachedAccount(account, options) {
  if (options === void 0) {
    options = {};
  }

  var endpoint = "/breachedaccount/" + encodeURIComponent(account) + "?";
  var params = [];

  if (options.domain) {
    params.push("domain=" + encodeURIComponent(options.domain));
  }

  if (options.truncate) {
    params.push('truncateResponse=true');
  }

  return (0, _fetchFromApi.default)("" + endpoint + params.join('&'));
};
/**
 * A module for retrieving breach data for a specific account.
 *
 * @module breachedAccount
 * @example
 * import { breachedAccount } from 'hibp';
 */


var _default = breachedAccount;
exports.default = _default;
//# sourceMappingURL=breachedAccount.js.map